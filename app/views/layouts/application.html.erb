<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Baiti" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <%= render "pages/components/header" %>
  <body>
    <% if notice %>
      <div id="flash-notice" class="fixed top-4 left-1/2 transform -translate-x-1/2 px-6 py-4 bg-green-200 border border-green-400 text-green-800 rounded shadow-lg z-50">
        <p class="alert alert-success text-center"><%= notice %></p>
      </div>
      <script>
        setTimeout(() => {
          const notice = document.getElementById("flash-notice");
          if (notice) {
            notice.style.opacity = '0';

            setTimeout(() => notice.remove(), 500);
          }
        }, 3000);
      </script>
    <% end %>
    <%if alert %>
      <div id="flash-alert" class="fixed top-4 left-1/2 transform -translate-x-1/2 px-6 py-4 bg-red-200 border border-red-400 text-red-800 rounded shadow-lg z-50">
        <p class="alert alert-danger"><%= alert %></p>
      </div>
      <script>
        setTimeout(() => {
          const alert = document.getElementById("flash-alert");
          if (alert) {
            alert.style.opacity = '0';

            setTimeout(() => alert.remove(), 500);
          }
        }, 6000);
      </script>
    <% end %>
    <main class="mt-16 sm:mt-18">
      <%= yield%>
    </main>
  </body>
  <%= render "pages/components/footer" %>
</html>
